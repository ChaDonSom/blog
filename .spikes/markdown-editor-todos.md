# Markdown editor todos

-   ways to get out of code blocks & quote blocks
-   code span within code block shows background, coloring, border-radius - should just be a code block
-   if you load from fresh, the newlines / new paragraphs have spacing; but after editing for a bit they get lost and compress down (maybe after having to do new-title weirdness to get out of a code block and/or quote block? I just now tried a code block and it happened after closing the code inline-intent. Although, it only happens for further blocks after the code span; Inserting a Zod link above still displayed the correct spacing.
-   code blocks \`\`\` don't make it through file-save-and-load. Also I bolded the Reification​ word above and it lost it after save-and-load.
-   inline things (links, code spans, strike-through, images; `[`, `` ` ``, `~~`)
-   pasting markdown
-   relative links (it can't find `../../` linked MD files)
-   better visual separation between heading levels, similar to GitHub markdown - maybe read through the CSS and organize it to my liking
-   'open' button for mobile
-   Ctrl+N not make a new window; Ctrl+shift+N make new window
-   fuzzy picker?
