{
  "name": "blog",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:sticky-headings": "node scripts/sync-sticky-headings.mjs",
    "postinstall": "npm run sync:sticky-headings"
  },
  "dependencies": {
    "@chadonsom/sticky-headings": "^0.1.1"
  }
}
